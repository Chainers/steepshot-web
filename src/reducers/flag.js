const initialState = {
  flags: [
   /* {
      index: 0,
      state: {true, false},
      isFlagLoading: {true, false},
      
      }*/
  ]
};

export default function flag(state = initialState, action) {
  switch (action.type) {
    
    default:
      return state;
  }
}
