import React from 'react';
import { dependencies, devDependencies } from '../../package.json';

const Activies = () => {
  return (
    <div>
      <p>Activies page</p>
    </div>
  );
};

export default Activies;
